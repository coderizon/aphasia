enum TrainingSessionState {
  listening,
  stopped,
  neverStarted;
}

enum TrainingSessionException {
  listening,
  noCurrentData,
  notListening,
  stopped,
  neverStarted;
}

enum ImageSource {
  www,
  local;
}
